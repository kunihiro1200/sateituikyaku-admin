# 実装計画

- [x] 1. バグ条件探索テストを作成（修正前）
  - **Property 1: Bug Condition** - 専任・一般・訪問後他決カテゴリのカウント数不一致
  - **重要**: このテストは修正前のコードで実行し、**失敗することを確認する**（失敗がバグの存在を証明）
  - **テストを修正したり、コードを修正したりしないこと**
  - **目的**: カウンターサンプルを発見し、バグの存在を確認する
  - **スコープ付きPBTアプローチ**: 決定論的バグのため、具体的な失敗ケースにプロパティをスコープする
  - テスト実装の詳細（バグ条件から）:
    - 専任カテゴリ: `isTodayOrBefore(nextCallDate)` を否定している（今日を含む）
    - 一般カテゴリ: `isTodayOrBefore(nextCallDate)` を否定している（今日を含む）
    - 訪問後他決カテゴリ: `isTodayOrBefore(nextCallDate)` を否定している（今日を含む）
  - テストアサーションは期待される動作プロパティと一致すべき:
    - サイドバーのカウント数 = フィルタリング結果の件数
    - 次電日が今日の売主は除外される
  - 修正前のコードで実行
  - **期待される結果**: テストが失敗する（これは正しい - バグが存在することを証明）
  - カウンターサンプルを文書化して根本原因を理解する
  - テストが作成され、実行され、失敗が文書化されたらタスク完了とマークする
  - _Requirements: 1.1, 1.2, 1.3_

- [x] 2. 保存プロパティテストを作成（修正前）
  - **Property 2: Preservation** - 他のカテゴリと基本条件の保存
  - **重要**: 観察優先の方法論に従う
  - 修正前のコードで非バグ入力の動作を観察
  - 保存要件から観察された動作パターンをキャプチャするプロパティベーステストを作成:
    - 他のカテゴリ（「当日TEL分」「未訪問他決」など）のカウントとフィルタリング結果が一致し続ける
    - 専任カテゴリの基本条件（専任他決打合せ ≠ "完了"、状況（当社） IN ("専任媒介", "他決→専任", "リースバック（専任）")）が保持される
    - 一般カテゴリの基本条件（専任他決打合せ ≠ "完了"、状況（当社） = "一般媒介"、契約年月 >= "2025-06-23"）が保持される
    - 訪問後他決カテゴリの基本条件（専任他決打合せ ≠ "完了"、状況（当社） IN ("他決→追客", "他決→追客不要", "一般→他決", "他社買取")、営担 ≠ ""）が保持される
  - プロパティベーステストはより強力な保存保証のために多くのテストケースを生成する
  - 修正前のコードでテストを実行
  - **期待される結果**: テストが成功する（ベースライン動作を確認）
  - テストが作成され、実行され、修正前のコードで成功したらタスク完了とマークする
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_

- [x] 3. 専任・一般・訪問後他決カテゴリのカウント数不一致を修正

  - [x] 3.1 修正を実装
    - フロントエンドの `isExclusive` 関数の条件を修正（`frontend/frontend/src/utils/sellerStatusFilters.ts`）
    - `isTodayOrBefore(nextCallDate)` の否定を、`normalizeDateString(nextCallDate) === todayStr` に変更
    - フロントエンドの `isGeneral` 関数の条件を修正
    - `isTodayOrBefore(nextCallDate)` の否定を、`normalizeDateString(nextCallDate) === todayStr` に変更
    - フロントエンドの `isVisitOtherDecision` 関数の条件を修正
    - `isTodayOrBefore(nextCallDate)` の否定を、`normalizeDateString(nextCallDate) === todayStr` に変更
    - _Bug_Condition: isBugCondition(input) where input.nextCallDate = 今日 AND (input.status IN ("専任媒介", "他決→専任", "リースバック（専任）", "一般媒介", "他決→追客", "他決→追客不要", "一般→他決", "他社買取"))_
    - _Expected_Behavior: サイドバーのカウント数 = フィルタリング結果の件数、次電日が今日の売主は除外される_
    - _Preservation: 他のカテゴリのカウントと表示、専任・一般・訪問後他決カテゴリの基本条件、GASとバックエンドのカウントロジック_
    - _Requirements: 2.1, 2.2, 2.3, 3.1, 3.2, 3.3, 3.4, 3.5_

  - [x] 3.2 バグ条件探索テストが成功することを確認
    - **Property 1: Expected Behavior** - 専任・一般・訪問後他決カテゴリのカウント数一致
    - **重要**: タスク1と同じテストを再実行する - 新しいテストを作成しない
    - タスク1のテストは期待される動作をエンコードしている
    - このテストが成功すると、期待される動作が満たされていることを確認する
    - タスク1のバグ条件探索テストを実行
    - **期待される結果**: テストが成功する（バグが修正されたことを確認）
    - _Requirements: 期待される動作プロパティ（デザインから）_

  - [x] 3.3 保存テストが引き続き成功することを確認
    - **Property 2: Preservation** - 他のカテゴリと基本条件の保存
    - **重要**: タスク2と同じテストを再実行する - 新しいテストを作成しない
    - タスク2の保存プロパティテストを実行
    - **期待される結果**: テストが成功する（リグレッションがないことを確認）
    - 修正後も全てのテストが成功することを確認（リグレッションなし）

- [x] 4. チェックポイント - 全てのテストが成功することを確認
  - 全てのテストが成功することを確認し、問題が発生した場合はユーザーに質問する
