# 実装計画: 通話モードページ訪問査定セクション「営担」フィールドのボタン選択式変更

## 概要

通話モードページ（`/sellers/:id/call`）の訪問査定セクションにある「営担」フィールドを、テキスト入力からボタン選択式に変更します。選択肢はスタッフ管理シート（`employees`テーブル）の`is_normal = true`のスタッフのイニシャルから取得します。

## タスク

- [x] 1. バックエンドAPIエンドポイントの確認と実装
  - `/api/employees/normal-initials` エンドポイントが既に実装されているか確認
  - 実装されていない場合は実装する（`is_normal = true` かつ `is_active = true` のスタッフのイニシャルをスタッフ名順に返す）
  - 認証ミドルウェアを適用
  - _要件: 1.1, 1.2, 1.4, 1.5_

- [x] 2. フロントエンド: 通常スタッフイニシャル取得機能の実装
  - [x] 2.1 状態管理の追加
    - `CallModePage.tsx` に `normalInitials` 状態を追加（`useState<string[]>([])`）
    - _要件: 1.2_
  
  - [x] 2.2 イニシャル取得ロジックの実装
    - `useEffect` で `/api/employees/normal-initials` からイニシャルを取得
    - エラー時は空配列を設定し、コンソールにエラーを出力
    - _要件: 1.1, 1.2, 1.3_
  
  - [ ]* 2.3 キャッシュ機構の実装
    - ローカルストレージに5分間キャッシュ
    - キャッシュが有効な場合はAPIリクエストをスキップ
    - _要件: 7.1, 7.2, 7.3, 7.4_

- [x] 3. フロントエンド: 営担ボタン選択UIの実装
  - [x] 3.1 テキスト入力フィールドを削除
    - 既存の `TextField` コンポーネントを削除
    - _要件: 制約事項3_
  
  - [x] 3.2 ボタン選択UIの実装
    - イニシャルボタンを動的に生成（`normalInitials.map()`）
    - 「クリア」ボタンを追加
    - 選択状態に応じてボタンスタイルを変更（`contained` / `outlined`）
    - _要件: 2.1, 2.2, 2.3, 2.4, 2.5_
  
  - [x] 3.3 イニシャル選択ハンドラーの実装
    - ボタンクリック時に `setEditedAssignedTo(initial)` を呼び出し
    - _要件: 3.1, 3.2, 3.3, 3.4_
  
  - [x] 3.4 クリアボタンハンドラーの実装
    - クリアボタンクリック時に `setEditedAssignedTo('')` を呼び出し
    - _要件: 4.1, 4.2, 4.3_

- [x] 4. エラーハンドリングの実装
  - [x] 4.1 通常スタッフが0件の場合の警告表示
    - `normalInitials.length === 0` の場合、警告メッセージを表示
    - _要件: 8.1_
  
  - [x] 4.2 API取得失敗時のエラーハンドリング
    - エラーをコンソールに出力
    - 空配列を状態に設定
    - _要件: 1.3, 8.2, 8.3_

- [x] 5. バックエンド: 営担検証ロジックの実装
  - [x] 5.1 保存時の営担検証
    - 営担が空文字列でない場合、通常スタッフのイニシャルであることを検証
    - 無効な営担の場合は400エラーを返す
    - _要件: 5.2, 6.1, 6.2, 6.3_
  
  - [x] 5.2 空文字列の場合の検証スキップ
    - 営担が空文字列の場合は検証をスキップ
    - _要件: 6.4_

- [x] 6. アクセシビリティの実装
  - [x] 6.1 キーボードナビゲーション
    - Tabキーでボタン間をフォーカス移動できるようにする
    - _要件: 9.1_
  
  - [x] 6.2 キーボード選択
    - Enterキーまたはスペースキーでボタンを選択できるようにする
    - _要件: 9.2_
  
  - [x] 6.3 フォーカスインジケーター
    - ボタンがフォーカスされている時にフォーカスインジケーターを表示
    - _要件: 9.3_

- [x] 7. チェックポイント - 基本機能の動作確認
  - 全てのテストが通ることを確認
  - ブラウザで通話モードページを開き、営担ボタン選択UIが正しく表示されることを確認
  - イニシャルボタンをクリックして営担を選択できることを確認
  - クリアボタンをクリックして営担をクリアできることを確認
  - 保存ボタンをクリックして営担が正しく保存されることを確認
  - 質問があれば、ユーザーに確認する

- [ ]* 8. テストの作成
  - [ ]* 8.1 単体テスト: 通常スタッフイニシャル取得
    - APIエンドポイントが正しくイニシャルを返すことをテスト
    - _要件: 1.1, 1.2, 1.4, 1.5_
  
  - [ ]* 8.2 単体テスト: 営担選択
    - 営担を選択できることをテスト
    - _要件: 3.1_
  
  - [ ]* 8.3 単体テスト: 営担クリア
    - 営担をクリアできることをテスト
    - _要件: 4.1_
  
  - [ ]* 8.4 統合テスト: エンドツーエンドフロー
    - 通常スタッフイニシャル取得から営担選択、保存までの一連の流れをテスト
    - _要件: 1.1, 2.1, 3.1, 5.1_

- [x] 9. 最終チェックポイント - 全機能の動作確認
  - 全てのテストが通ることを確認
  - ブラウザで通話モードページを開き、全ての機能が正しく動作することを確認
  - エラーハンドリングが正しく動作することを確認
  - アクセシビリティが正しく実装されていることを確認
  - 質問があれば、ユーザーに確認する

## 注意事項

- タスクに `*` が付いているものはオプションです（スキップ可能）
- 各タスクは前のタスクが完了してから実行してください
- チェックポイントでは、必ずユーザーに確認してから次に進んでください
- 不明な点があれば、必ずユーザーに確認してください

---

**作成日**: 2026年4月1日  
**作成者**: KIRO AI Assistant
