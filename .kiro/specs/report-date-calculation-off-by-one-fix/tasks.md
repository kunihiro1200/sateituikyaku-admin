# 実装計画

- [x] 1. バグ条件探索テストを作成
  - **Property 1: Bug Condition** - 報告日が本日+13日になるバグ
  - **重要**: このテストは修正前のコードで**必ず失敗する必要があります** - 失敗によりバグの存在が確認されます
  - **修正を試みないでください**: テストが失敗してもコードを修正しないでください
  - **注意**: このテストは期待される動作をエンコードしています - 実装後にパスすることで修正を検証します
  - **目標**: バグを実証する反例を表面化させる
  - **スコープ付きPBTアプローチ**: 決定論的バグのため、プロパティを具体的な失敗ケースにスコープして再現性を確保
  - 「報告日設定」が「する」の場合、報告日が本日+13日になることをテスト（Bug Conditionから）
  - テストのアサーションは設計書のExpected Behavior Propertiesと一致させる（本日+14日であるべき）
  - 修正前のコードでテストを実行
  - **期待される結果**: テスト失敗（これは正しい - バグの存在を証明）
  - 反例を文書化して根本原因を理解する
  - テストが作成され、実行され、失敗が文書化されたらタスク完了とする
  - _Requirements: 1.1, 1.2_

- [x] 2. 保存プロパティテストを作成（修正実装前）
  - **Property 2: Preservation** - 手動入力と「しない」設定の動作保持
  - **重要**: 観察優先の方法論に従う
  - 観察: 修正前のコードで非バグ入力（「報告日設定」が「しない」または手動入力）の動作を観察
  - 観察: 「しない」設定の物件は報告日が自動計算されない
  - 観察: 手動入力された報告日はそのまま保持される
  - Preservation Requirementsから、全ての非バグ条件入力で動作が保持されることをproperty-based testで記述
  - プロパティベーステストは多くのテストケースを自動生成し、より強力な保証を提供
  - 修正前のコードでテストを実行
  - **期待される結果**: テスト成功（ベースライン動作を確認）
  - 修正前のコードでテストがパスしたらタスク完了とする
  - _Requirements: 3.1, 3.2, 3.3_

- [-] 3. 報告日計算の1日ずれバグの修正

  - [x] 3.1 Glideアプリの「報告日」カラム設定を修正（ユーザー作業）
    - **場所**: Glideアプリの「報告日」カラム設定
    - **現在の設定**: Initial value = `IF(ISNOTBLANK([_THISROW_BEFORE].[報告日]), [_THISROW_BEFORE].[報告日] + 14, TODAY() + 14)`
    - **修正後の設定**: Initial value = `TODAY() + 14`
    - **手順**:
      1. Glideアプリの編集画面を開く
      2. 「報告日」カラムを選択
      3. Initial valueを `TODAY() + 14` に変更
      4. 保存
    - **AA12497の修正**:
      - 現在の間違った値（4月13日）を削除または正しい値（4月14日）に修正
      - または、「報告日設定」を一度「しない」に変更してから「する」に戻して再計算させる
    - _Bug_Condition: isBugCondition(input) where input.reportDateSetting = "する" AND input.calculatedDate = TODAY() + 13_
    - _Expected_Behavior: result = TODAY() + 14 AND NOT (result = TODAY() + 13)_
    - _Preservation: 「報告日設定」が「しない」または空欄、または手動入力された報告日は変更されない_
    - _Requirements: 1.1, 1.2, 2.1, 2.2, 3.1, 3.2, 3.3_

  - [ ] 3.2 バグ条件探索テストが成功することを確認
    - **Property 1: Expected Behavior** - 報告日が本日+14日で正確に計算される
    - **重要**: タスク1と同じテストを再実行 - 新しいテストを書かない
    - タスク1のテストは期待される動作をエンコードしている
    - このテストがパスすれば、期待される動作が満たされていることを確認
    - タスク1のバグ条件探索テストを再実行
    - **期待される結果**: テスト成功（バグが修正されたことを確認）
    - _Requirements: 2.1, 2.2_

  - [ ] 3.3 保存テストが引き続き成功することを確認
    - **Property 2: Preservation** - 手動入力と「しない」設定の動作保持
    - **重要**: タスク2と同じテストを再実行 - 新しいテストを書かない
    - タスク2の保存プロパティテストを再実行
    - **期待される結果**: テスト成功（リグレッションがないことを確認）
    - 修正後も全てのテストがパスすることを確認（リグレッションなし）

- [ ] 4. チェックポイント - 全てのテストがパスすることを確認
  - 全てのテストがパスすることを確認し、疑問があればユーザーに質問する
