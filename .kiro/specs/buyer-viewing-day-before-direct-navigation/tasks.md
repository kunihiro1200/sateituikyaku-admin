# Implementation Tasks

## Overview

本タスクリストは、買主リストの「内覧日前日」カテゴリから案件をクリックした際に、内覧ページを直接開く機能の実装タスクを記述します。

**重要**: この機能は**既に実装済み**です。本タスクリストは、既存実装の確認と検証のためのものです。

## Task List

### Phase 1: 既存実装の確認

- [x] 1. 既存実装の確認
  - [x] 1.1 `BuyersPage.tsx`の`handleRowClick`関数を確認
  - [x] 1.2 `categoryKeyToDisplayName`マッピングを確認
  - [x] 1.3 ルーティング設定を確認
  - [x] 1.4 バックエンドの`BuyerStatusCalculator.ts`との整合性を確認

### Phase 2: 動作検証（推奨）

- [ ] 2. 手動テストの実施
  - [ ] 2.1 「内覧日前日」カテゴリからの遷移を確認
    - サイドバーで「②内覧日前日」を選択
    - 買主行をクリック
    - 内覧ページ（`/buyers/:id/viewing-result`）が開くことを確認
  - [ ] 2.2 その他のカテゴリからの遷移を確認
    - サイドバーで「当日TEL」を選択
    - 買主行をクリック
    - 詳細画面（`/buyers/:id`）が開くことを確認
  - [ ] 2.3 カテゴリ未選択時の遷移を確認
    - サイドバーで「All」を選択
    - 買主行をクリック
    - 詳細画面（`/buyers/:id`）が開くことを確認

### Phase 3: テストの追加（オプション）

- [ ]* 3. ユニットテストの追加
  - [ ]* 3.1 `handleRowClick`関数のテストを追加
    - 「内覧日前日」カテゴリの場合のテスト
    - その他のカテゴリの場合のテスト
    - カテゴリ未選択の場合のテスト
  - [ ]* 3.2 `categoryKeyToDisplayName`マッピングのテストを追加
    - マッピングの存在確認
    - バックエンドとの整合性確認

- [ ]* 4. 統合テストの追加
  - [ ]* 4.1 サイドバーからの遷移テストを追加
    - 「内覧日前日」カテゴリからの遷移
    - その他のカテゴリからの遷移
    - カテゴリ未選択時の遷移

### Phase 4: ドキュメント整備（オプション）

- [ ]* 5. コードコメントの追加
  - [ ]* 5.1 `handleRowClick`関数にコメントを追加
  - [ ]* 5.2 `categoryKeyToDisplayName`マッピングにコメントを追加

- [ ]* 6. ユーザードキュメントの作成
  - [ ]* 6.1 操作マニュアルの作成
  - [ ]* 6.2 FAQ の作成

## Task Status Legend

- `[ ]` - Not started
- `[x]` - Completed
- `[ ]*` - Optional task

## Notes

### 既存実装の場所

- **ファイル**: `frontend/frontend/src/pages/BuyersPage.tsx`
- **実装箇所**:
  - `categoryKeyToDisplayName`マッピング（85-95行目）
  - `handleRowClick`関数（334-342行目）

### 実装の品質

既存実装は以下の点で優れています：

1. **拡張性**: 新しいカテゴリを追加しやすい設計
2. **可読性**: コードが明確で理解しやすい
3. **保守性**: マッピングを一箇所で管理
4. **整合性**: バックエンドのカテゴリキーと一致

### 推奨される次のステップ

1. **Phase 2（動作検証）を実施**: 既存実装が正しく動作することを確認
2. **Phase 3（テストの追加）を検討**: 将来的な保守性を向上させるため
3. **Phase 4（ドキュメント整備）を検討**: ユーザーと開発者の理解を深めるため

## Completion Criteria

### 必須

- [x] Phase 1: 既存実装の確認が完了している

### 推奨

- [ ] Phase 2: 手動テストが完了し、全てのテストケースが成功している

### オプション

- [ ] Phase 3: ユニットテスト・統合テストが追加されている
- [ ] Phase 4: コードコメント・ユーザードキュメントが整備されている

## Timeline

- **Phase 1**: 完了済み（既存実装の確認）
- **Phase 2**: 推奨（30分程度）
- **Phase 3**: オプション（2-3時間程度）
- **Phase 4**: オプション（1-2時間程度）

## Dependencies

なし（既存実装のため）

## Risks

なし（既存実装のため）

## Success Metrics

1. **機能の正確性**: 「内覧日前日」カテゴリから買主をクリックした際、内覧ページが開く
2. **既存機能の維持**: その他のカテゴリからの遷移は詳細画面を開く
3. **ユーザー満足度**: ユーザーが期待通りの動作をすることを確認

## Conclusion

本タスクリストは、既存実装の確認と検証を目的としています。Phase 1は完了済みであり、Phase 2（動作検証）を実施することを推奨します。Phase 3とPhase 4はオプションですが、将来的な保守性とユーザー理解を向上させるために検討することを推奨します。
