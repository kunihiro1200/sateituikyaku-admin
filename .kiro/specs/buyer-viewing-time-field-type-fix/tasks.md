# 実装計画

- [ ] 1. バグ条件の探索テストを作成する
  - **Property 1: Bug Condition** - 内覧時間フィールドが `type="time"` を持たないバグ
  - **重要**: このテストは未修正コードで**失敗する**こと — 失敗がバグの存在を証明する
  - **修正前にテストを実行してもコードを修正しないこと**
  - **目的**: バグが存在することを示す反例を見つける
  - **スコープ付きPBTアプローチ**: 決定論的なバグのため、具体的な失敗ケースにスコープを絞る
  - `NewBuyerPage` をレンダリングし、「内覧時間」フィールドの `input` 要素の `type` 属性を確認する
  - テスト内容（design.md の Bug Condition より）:
    - 「内覧時間」フィールドの `input` 要素の `type` 属性が `"time"` であることをアサートする（未修正コードでは `"text"` のため失敗する）
    - `InputLabelProps={{ shrink: true }}` が適用されていることをアサートする（未修正コードでは未指定のため失敗する）
    - 隣の「内覧日」フィールドが `type="date"` を持つことを確認する（参照実装として）
  - 未修正コードでテストを実行する
  - **期待される結果**: テストが**失敗**する（これが正しい — バグの存在を証明する）
  - 見つかった反例を記録する（例: 「内覧時間フィールドの `type` が `"text"` になっている」）
  - テストを作成し、実行し、失敗を記録したらタスク完了とする
  - _Requirements: 1.1, 1.2_

- [ ] 2. 保全プロパティテストを作成する（修正前に実施）
  - **Property 2: Preservation** - 既存のフォーム動作が維持される
  - **重要**: 観察優先メソドロジーに従うこと
  - 未修正コードで非バグ条件の入力（`type="time"` が指定されていない状態でも動作する部分）の動作を観察する
  - 観察内容（design.md の Preservation Requirements より）:
    - 「内覧日」フィールドが `type="date"` で正常に動作することを確認
    - "14:00" を入力して登録した場合、`viewing_time: "14:00"` がAPIに送信されることを確認
    - フィールドを空のまま登録した場合、`viewing_time: null` がAPIに送信されることを確認
    - `onChange` ハンドラが `viewingTime` state を正しく更新することを確認
  - プロパティベーステスト: ランダムな時間文字列（"00:00"〜"23:59"）を入力した場合、`viewingTime` state が正しく更新されることを確認
  - 未修正コードでテストを実行する
  - **期待される結果**: テストが**成功**する（これが保全すべきベースライン動作を確認する）
  - テストを作成し、実行し、未修正コードで成功することを確認したらタスク完了とする
  - _Requirements: 3.1, 3.2, 3.3_

- [ ] 3. 内覧時間フィールドの型バグを修正する

  - [ ] 3.1 修正を実装する
    - `frontend/frontend/src/pages/NewBuyerPage.tsx` の約1102行目の「内覧時間」TextField を修正する
    - `type="time"` を追加する（ブラウザ標準の時間入力UIを有効化）
    - `InputLabelProps={{ shrink: true }}` を追加する（ラベルを常に上部に固定）
    - `placeholder="例: 14:00"` を削除する（`type="time"` ではブラウザが独自のプレースホルダーを表示するため不要）
    - _Bug_Condition: isBugCondition(field) — field.props に "type" が存在しない OR field.props.type != "time"_
    - _Expected_Behavior: 修正後の TextField は `type="time"` プロパティを持ち、ブラウザ標準の時間ピッカーUIを提供する_
    - _Preservation: 「内覧日」フィールドの動作、`viewing_time` 値のAPI送信、空値時の `null` 送信、`onChange` ハンドラは変更しない_
    - _Requirements: 2.1, 2.2, 3.1, 3.2, 3.3_

  - [ ] 3.2 バグ条件の探索テストが成功することを確認する
    - **Property 1: Expected Behavior** - 内覧時間フィールドが `type="time"` として動作する
    - **重要**: タスク1で作成した**同じテスト**を再実行する — 新しいテストを書かないこと
    - タスク1のテストは期待される動作をエンコードしている
    - このテストが成功すれば、期待される動作が満たされたことを確認できる
    - タスク1のバグ条件探索テストを実行する
    - **期待される結果**: テストが**成功**する（バグが修正されたことを確認）
    - _Requirements: 2.1, 2.2_

  - [ ] 3.3 保全テストが引き続き成功することを確認する
    - **Property 2: Preservation** - 既存のフォーム動作が維持される
    - **重要**: タスク2で作成した**同じテスト**を再実行する — 新しいテストを書かないこと
    - タスク2の保全プロパティテストを実行する
    - **期待される結果**: テストが**成功**する（リグレッションがないことを確認）
    - 修正後もすべてのテストが成功することを確認する（リグレッションなし）

- [ ] 4. チェックポイント — すべてのテストが成功することを確認する
  - すべてのテストが成功することを確認する。疑問が生じた場合はユーザーに確認する。
