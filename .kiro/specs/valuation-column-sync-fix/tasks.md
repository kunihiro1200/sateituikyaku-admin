# Implementation Plan

- [x] 1. バグ条件探索テストを作成（修正前）
  - **Property 1: Bug Condition** - 査定額同期先列の間違い
  - **重要**: このテストは修正前のコードで実行し、**失敗することを確認する**（失敗がバグの存在を証明）
  - **修正後は実行しない** - このテストは探索用であり、修正後の検証には使用しない
  - **目標**: バグの存在を確認し、根本原因を理解する
  - **スコープ付きPBTアプローチ**: 決定的なバグのため、具体的な失敗ケースに範囲を限定
  - テスト内容: `column-mapping.json`の`databaseToSpreadsheet`セクションで、`valuation_amount_1/2/3`が「査定額1/2/3」（CB/CC/CD列）にマッピングされていることを確認
  - 修正前のコードで実行
  - **期待される結果**: テストが**失敗する**（これが正しい - バグが存在することを証明）
  - 失敗例を記録: `valuation_amount_1`が「査定額1」（CB列）にマッピングされている
  - タスク完了条件: テストを作成し、実行し、失敗を記録したら完了
  - _Requirements: 1.1, 1.2, 1.3_

- [x] 2. 保存検証テストを作成（修正前）
  - **Property 2: Preservation** - 既存同期ロジックの維持
  - **重要**: 観察優先の方法論に従う
  - 修正前のコードで非バグ条件のケース（査定額以外のフィールド）の動作を観察
  - 観察した動作パターンをキャプチャするプロパティベーステストを作成
  - プロパティベーステストは多くのテストケースを自動生成し、より強力な保証を提供
  - 修正前のコードでテストを実行
  - **期待される結果**: テストが**成功する**（ベースライン動作を確認）
  - タスク完了条件: テストを作成し、修正前のコードで成功することを確認したら完了
  - _Requirements: 3.1, 3.2, 3.3, 3.4_

- [x] 3. 査定額同期先列の修正

  - [x] 3.1 `column-mapping.json`のマッピングを修正
    - `valuation_amount_1`を「査定額1」から「査定額1（自動計算）v」に変更
    - `valuation_amount_2`を「査定額2」から「査定額2（自動計算）v」に変更
    - `valuation_amount_3`を「査定額3」から「査定額3（自動計算）v」に変更
    - _Bug_Condition: isBugCondition(columnMapping) where columnMapping.databaseToSpreadsheet['valuation_amount_1'] == '査定額1'_
    - _Expected_Behavior: columnMapping.databaseToSpreadsheet['valuation_amount_1'] == '査定額1（自動計算）v'_
    - _Preservation: 査定額以外のフィールドのマッピングは変更しない_
    - _Requirements: 2.1, 2.2, 2.3, 2.4_

  - [x] 3.2 `seller-spreadsheet-column-mapping.md`を更新
    - 査定額1/2/3の列位置を「列79-81（CB/CC/CD）」から「列54-56（BC/BD/BE）」に修正
    - カラム名を「査定額1/2/3」から「査定額1（自動計算）v/査定額2（自動計算）v/査定額3（自動計算）v」に修正
    - 「自動計算値」であることを明記
    - _Requirements: 2.4_

  - [x] 3.3 修正後の動作確認
    - **Property 1: Expected Behavior** - 正しい同期先列への修正
    - **重要**: タスク1で作成したテストは実行しない（探索用のため）
    - 代わりに、手動で以下を確認:
      - `column-mapping.json`で`valuation_amount_1/2/3`が「査定額1（自動計算）v」等にマッピングされていることを確認
      - スプレッドシートでBC/BD/BE列に自動計算値がある売主を選択
      - 同期を実行し、DBの`valuation_amount_1/2/3`がBC/BD/BE列の値（万円→円変換後）になることを確認
    - **期待される結果**: 正しい列から査定額が同期される
    - _Requirements: 2.1, 2.2, 2.3_

  - [x] 3.4 保存検証テストを再実行
    - **Property 2: Preservation** - 既存同期ロジックの維持
    - **重要**: タスク2で作成したテストを再実行する（新しいテストは作成しない）
    - タスク2で作成したテストを修正後のコードで実行
    - **期待される結果**: テストが**成功する**（リグレッションがないことを確認）
    - 査定額以外のフィールドの同期、優先順位ロジック、単位変換ロジックが正しく動作することを確認
    - _Requirements: 3.1, 3.2, 3.3, 3.4_

- [x] 4. チェックポイント - 全テストが成功することを確認
  - 全てのテストが成功することを確認
  - 質問があればユーザーに確認
