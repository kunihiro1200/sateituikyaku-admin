# 物件リスト・業務依頼 自動同期バグ修正 タスクリスト

## Tasks

- [x] 1. 探索的バグ確認テストの作成と実行
  - [x] 1.1 未修正コードで `startServer()` が業務依頼の定期同期をスケジュールしないことを確認するテストを作成する
  - [x] 1.2 テストを実行してバグを確認する（失敗することを期待）

- [x] 2. バグ修正の実装
  - [x] 2.1 `WorkTaskSyncService` に `isSyncing` フラグと `isSyncInProgress()` メソッドを追加し、`syncAll()` 実行中はフラグを立てる
  - [x] 2.2 `backend/src/index.ts` の `startServer()` 内に業務依頼定期同期スケジューラーを追加する（40秒後起動、`WORK_TASK_SYNC_INTERVAL_MINUTES` 環境変数対応）

- [x] 3. 修正確認テストの作成と実行
  - [x] 3.1 修正後に業務依頼の定期同期が自動起動することを確認するテストを作成する
  - [x] 3.2 手動同期・売主同期・買主同期・物件リスト同期が引き続き正常に動作することを確認するテストを作成する
  - [x] 3.3 全テストを実行して修正を確認する
