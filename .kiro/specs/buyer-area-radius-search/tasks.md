# 実装計画：買主エリア半径検索機能

## 概要

買主リストの「他社物件新着配信」機能を改善し、エリア選択をボタン形式からテキスト入力形式に変更します。ユーザーが住所を手入力すると、Google Maps APIを使用してその住所を地理座標に変換し、半径3km圏内で条件に合致する買主を自動的に検索・表示します。

## タスク

- [x] 1. データベースマイグレーションとインデックス作成
  - `backend/migrations/XXX_add_buyer_area_coordinates.sql`を作成
  - `buyers`テーブルに`desired_area_lat`（DOUBLE PRECISION）カラムを追加
  - `buyers`テーブルに`desired_area_lng`（DOUBLE PRECISION）カラムを追加
  - 地理座標インデックス`idx_buyers_desired_area_coordinates`を作成
  - マイグレーションを実行してデータベースに反映
  - _要件: 3.1, 9.2_

- [x] 2. GeocodingServiceの実装
  - [x] 2.1 GeocodingServiceクラスの作成
    - `backend/src/services/GeocodingService.ts`を作成
    - Google Maps API Keyを環境変数から取得
    - _要件: 2.1, 2.2_
  
  - [x] 2.2 geocodeAddressメソッドの実装
    - Google Maps Geocoding APIを呼び出して住所を緯度経度に変換
    - エラーハンドリング（APIエラー、無効な住所）
    - _要件: 2.1, 2.2, 2.3_
  
  - [ ]* 2.3 geocodeAddressのプロパティテスト
    - **プロパティ1: ジオコーディング成功**
    - **検証要件: 2.1**
    - 有効な日本の住所に対して緯度経度が返されることを確認
    - 緯度が-90〜90、経度が-180〜180の範囲内であることを確認
  
  - [x] 2.4 calculateDistanceメソッドの実装（Haversine公式）
    - 2点間の地理座標から距離（km）を計算
    - _要件: 3.2, 3.3_
  
  - [ ]* 2.5 calculateDistanceのプロパティテスト
    - **プロパティ2: Haversine公式の正確性**
    - **検証要件: 3.3**
    - 既知の距離を持つ2点の座標ペアで距離を計算
    - 計算結果が既知の距離と一致することを確認（誤差±1%以内）
    - 距離は常に非負であることを確認

- [x] 3. BuyerServiceの拡張
  - [x] 3.1 getBuyersByRadiusSearchメソッドの実装
    - 住所を地理座標に変換（GeocodingServiceを使用）
    - 半径3km圏内の買主を検索
    - 最新状況でフィルタリング（"買"、"D"を除外）
    - 物件種別でフィルタリング
    - 価格帯でフィルタリング
    - 距離でソート（近い順）
    - キャッシュ機能の実装（TTL: 10分）
    - _要件: 3.1, 3.2, 3.4, 4.1, 4.2, 4.3, 5.3, 6.2, 6.3, 6.5_
  
  - [ ]* 3.2 半径検索のプロパティテスト
    - **プロパティ3: 半径検索**
    - **検証要件: 3.1, 3.2**
    - 結果に含まれる全ての買主の距離が3km以下であることを確認
    - 結果に含まれない買主の距離が3kmより大きいことを確認
  
  - [ ]* 3.3 距離順ソートのプロパティテスト
    - **プロパティ4: 距離順ソート**
    - **検証要件: 3.4**
    - 検索結果がソート済みであることを確認（i番目の距離 ≤ i+1番目の距離）
  
  - [ ]* 3.4 最新状況フィルタリングのプロパティテスト
    - **プロパティ5: 最新状況フィルタリング**
    - **検証要件: 4.1, 4.2, 4.3**
    - 結果に"買"を含む買主が含まれないことを確認
    - 結果に"D"の買主が含まれないことを確認
    - 結果に空白またはnullの買主が含まれることを確認
  
  - [ ]* 3.5 物件種別フィルタリングのプロパティテスト
    - **プロパティ6: 物件種別フィルタリング**
    - **検証要件: 5.3**
    - 結果に含まれる全ての買主が選択された種別を希望していることを確認
  
  - [ ]* 3.6 価格帯フィルタリングのプロパティテスト
    - **プロパティ7: 価格帯フィルタリング**
    - **検証要件: 6.2, 6.3, 6.5**
    - 結果に含まれる全ての買主の希望価格範囲が指定価格帯と重複することを確認
    - 結果に含まれない買主の希望価格範囲が指定価格帯と重複しないことを確認

- [ ] 4. チェックポイント - コア機能の動作確認
  - GeocodingServiceが正しく動作することを確認
  - BuyerServiceの半径検索が正しく動作することを確認
  - 全てのテストが通ることを確認
  - 質問があればユーザーに確認

- [x] 5. バックエンドAPIエンドポイントの追加
  - [x] 5.1 POST /api/buyers/radius-search エンドポイントの実装
    - `backend/src/routes/buyers.ts`に新しいエンドポイントを追加
    - リクエストボディのバリデーション（address, propertyTypes必須）
    - BuyerService.getBuyersByRadiusSearchを呼び出し
    - エラーハンドリング（ジオコーディングエラー、データベースエラー）
    - レート制限の設定（1分間に10リクエスト）
    - _要件: 1.1, 1.2, 1.3, 1.4, 2.3, 10.1, 10.2, 10.3, 10.4_
  
  - [ ]* 5.2 APIエンドポイントの統合テスト
    - 有効な住所で検索結果が返されることを確認
    - 無効な住所で400エラーが返されることを確認
    - 物件種別未選択で400エラーが返されることを確認

- [x] 6. フロントエンドUIの変更
  - [x] 6.1 OtherCompanyDistributionPage.tsxの修正
    - エリア選択ドロップダウンを住所入力テキストボックスに変更
    - 住所入力フィールドのバリデーション（最大200文字）
    - APIエンドポイントを`/api/buyers/radius-search`に変更
    - ローディングインジケーターの表示
    - エラーメッセージの表示（ジオコーディングエラー、ネットワークエラー）
    - 検索結果に距離を表示
    - _要件: 1.1, 1.2, 1.3, 1.4, 7.1, 7.2, 7.3, 7.4, 7.5, 8.1, 8.2, 8.3, 8.4, 9.1, 9.4, 10.1, 10.2, 10.3_
  
  - [ ]* 6.2 フロントエンドの単体テスト
    - 住所入力フィールドが正しく表示されることを確認
    - 検索ボタンをクリックするとAPIが呼び出されることを確認
    - エラーメッセージが正しく表示されることを確認

- [ ] 7. チェックポイント - エンドツーエンドの動作確認
  - フロントエンドからバックエンドまでの全体フローが動作することを確認
  - 住所を入力して検索結果が表示されることを確認
  - エラーハンドリングが正しく動作することを確認
  - 質問があればユーザーに確認

- [ ] 8. 地理座標初期データ投入バッチ処理
  - [ ] 8.1 backfill-buyer-coordinates.tsスクリプトの作成
    - `backend/scripts/backfill-buyer-coordinates.ts`を作成
    - `desired_area`が存在し、座標が未設定の買主を取得
    - `desired_area`から住所を抽出（extractAddress関数）
    - GeocodingServiceで地理座標を取得
    - データベースに座標を保存
    - レート制限対策（100ms遅延）
    - 成功・失敗カウントのログ出力
    - _要件: 3.1_
  
  - [ ] 8.2 バッチ処理の実行
    - スクリプトを実行して既存買主データに地理座標を設定
    - 実行結果をログで確認

- [ ] 9. 環境変数の設定
  - [ ] 9.1 Google Maps API Keyの取得
    - Google Cloud Consoleで新しいAPIキーを作成
    - Geocoding APIを有効化
    - APIキーの制限を設定（HTTPリファラー、IPアドレス）
    - _要件: 2.2_
  
  - [ ] 9.2 Vercel環境変数の設定
    - Vercelダッシュボードで`GOOGLE_MAPS_API_KEY`を設定
    - 本番環境とプレビュー環境の両方に設定

- [x] 10. デプロイと動作確認
  - [x] 10.1 バックエンドのデプロイ
    - `backend/`ディレクトリをVercelにデプロイ
    - デプロイログを確認してエラーがないことを確認
  
  - [x] 10.2 フロントエンドのデプロイ
    - `frontend/frontend/`ディレクトリをVercelにデプロイ
    - デプロイログを確認してエラーがないことを確認
  
  - [x] 10.3 本番環境での動作確認
    - 他社物件新着配信ページにアクセス
    - 住所を入力して検索
    - 検索結果が正しく表示されることを確認
    - エラーハンドリングが正しく動作することを確認
    - パフォーマンスを確認（3秒以内に結果が表示される）
    - _要件: 9.1_

- [x] 11. 最終チェックポイント
  - 全ての要件が満たされていることを確認
  - 全てのテストが通ることを確認
  - ドキュメントが最新であることを確認
  - ユーザーに最終確認を依頼

## 注意事項

- タスクに`*`が付いているものはオプション（テスト関連）で、スキップ可能です
- 各タスクは前のタスクに依存しているため、順番に実行してください
- チェックポイントでは必ず動作確認を行い、問題があれば前のタスクに戻ってください
- プロパティベーステストはfast-checkライブラリを使用します（最低100回イテレーション）
- Google Maps APIのレート制限（1秒あたり50リクエスト）に注意してください
