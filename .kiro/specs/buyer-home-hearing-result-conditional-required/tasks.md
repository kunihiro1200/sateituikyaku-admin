# 実装タスク: 持家ヒアリング結果の条件付き必須バリデーション

## 概要

買主詳細ページ（BuyerDetailPage.tsx）において、「持家ヒアリング結果」フィールドを条件付き必須項目として実装します。

受付日が2026年3月30日以降、かつ「問合時持家ヒアリング」フィールドが空白でない場合に、「持家ヒアリング結果」フィールドの入力を必須とします。

---

## タスク

- [x] 1. isHomeHearingResultRequired 関数の修正
  - `frontend/frontend/src/pages/BuyerDetailPage.tsx` の `isHomeHearingResultRequired` 関数に受付日の条件を追加
  - 受付日が2026-03-30以降かつ問合時持家ヒアリングが空白でない場合にtrueを返すよう修正
  - 日付比較ロジックを実装（`new Date(data.reception_date) >= new Date('2026-03-30')`）
  - エラーハンドリングを追加（Invalid Dateのチェック）
  - _要件: 1.1, 1.5, 2.1, 2.2, 2.3, 2.4_

- [ ]* 1.1 isHomeHearingResultRequired 関数のユニットテストを作成
  - **Property 1: 受付日境界値テスト**
  - **Validates: 要件 1.5, 2.1**
  - 受付日が2026-03-30の場合にtrueを返すことを確認
  - 受付日が2026-03-29の場合にfalseを返すことを確認
  - 受付日がnullの場合にfalseを返すことを確認

- [x] 2. 初期表示時の必須フィールドチェック
  - `fetchBuyer()` 関数内の初期表示時チェックロジックを確認
  - `isHomeHearingResultRequired(res.data)` の判定結果に基づいて `initialMissing` に追加
  - 持家ヒアリング結果が空の場合のみ `owned_home_hearing_result` を追加
  - _要件: 8.1, 8.2, 8.3_

- [x] 3. 問合時持家ヒアリング変更時の動的バリデーション
  - [x] 3.1 問合時持家ヒアリングのフィールド変更ハンドラーを実装
    - `handleFieldChange` 呼び出し時に `isHomeHearingResultRequired` を再判定
    - 必須条件を満たす場合、持家ヒアリング結果が空なら `missingRequiredFields` に追加
    - 必須条件を満たさない場合、`missingRequiredFields` から削除
    - _要件: 4.1, 4.2, 4.3, 4.4_
  
  - [x] 3.2 問合時持家ヒアリングの「不要」「未」選択時の処理
    - 「不要」または「未」が選択された場合、持家ヒアリング結果フィールドを非表示
    - 非表示時は必須バリデーションを発動しない
    - _要件: 7.1, 7.2, 7.3, 7.5_

- [x] 4. 持家ヒアリング結果変更時の動的バリデーション
  - 持家ヒアリング結果の4択ボタンクリック時のハンドラーを実装
  - 値が入力された場合、`missingRequiredFields` から `owned_home_hearing_result` を削除
  - 値が空で必須条件を満たす場合、`missingRequiredFields` に追加
  - _要件: 5.1, 5.2, 5.3_

- [x] 5. checkMissingFields 関数の確認
  - `checkMissingFields()` 関数内の持家ヒアリング結果チェックロジックを確認
  - `isHomeHearingResultRequired(buyer)` が true の場合のみチェック
  - 既存の必須チェックロジックとの重複がないことを確認
  - _要件: 1.1, 1.4, 6.1, 6.2, 6.3, 6.4_

- [x] 6. UI ハイライト表示の実装
  - 持家ヒアリング結果の4択ボタンに赤枠スタイルを追加
  - `missingRequiredFields.has('owned_home_hearing_result')` が true の場合にハイライト
  - 選択されていないボタンに `border: '2px solid red'` を適用
  - _要件: 3.1, 3.2, 3.3, 3.4_

- [x] 7. 条件付き表示ロジックの実装
  - 問合時持家ヒアリングが空、「不要」、「未」の場合に持家ヒアリング結果フィールドを非表示
  - 非表示時は必須バリデーションを発動しない
  - _要件: 7.1, 7.2, 7.3, 7.4, 7.5_

- [x] 8. Checkpoint - 動作確認とテスト
  - ブラウザで買主詳細ページを開き、以下のシナリオをテスト
  - シナリオ1: 受付日が2026-04-01、問合時持家ヒアリングが「Y」、持家ヒアリング結果が空 → 赤枠ハイライト表示
  - シナリオ2: 問合時持家ヒアリングを「Y」に変更 → 持家ヒアリング結果が赤枠ハイライト表示
  - シナリオ3: 持家ヒアリング結果を「持家（マンション）」に変更 → 赤枠ハイライト解除
  - シナリオ4: 保存ボタンをクリック（持家ヒアリング結果が空） → ValidationWarningDialog 表示
  - シナリオ5: 問合時持家ヒアリングを「不要」に変更 → 持家ヒアリング結果フィールドが非表示
  - Ensure all tests pass, ask the user if questions arise.

- [ ]* 9. 統合テストの作成
  - 初期表示時のハイライトテスト
  - 問合時持家ヒアリング変更時の動的バリデーションテスト
  - 持家ヒアリング結果入力時のハイライト解除テスト
  - 保存時のバリデーションテスト
  - 条件付き表示テスト

- [x] 10. エンコーディング保護の確認
  - 日本語文字列を含むファイル編集後、`getDiagnostics` でエラーがないか確認
  - ビルドエラーが発生していないことを確認
  - _要件: 9.1, 9.2, 9.3_

---

## 注意事項

### 日本語ファイル編集時のエンコーディング保護

**重要**: `BuyerDetailPage.tsx` は日本語文字列を含むため、`strReplace` ツールでの直接編集は禁止です。

**推奨方法**:
1. `git restore` で正常なUTF-8ファイルを取得
2. Pythonスクリプトで変更を適用（UTF-8で書き込み）
3. `getDiagnostics` でエラーがないか確認
4. コミット＆プッシュ

### 既存の必須バリデーションとの共存

- `checkMissingFields()` 関数内で `owned_home_hearing_result` が重複して追加されないこと
- 既存の必須フィールド（`inquiry_source`、`latest_status`、`distribution_type` 等）のバリデーションが正常に動作すること

### 条件式の一貫性

スプレッドシートの条件式と完全に一致させること:
```
AND([受付日]>="2026/3/30", ISNOTBLANK([問合時持家ヒアリング]))
```

### 「不要」「未」の扱い

「不要」「未」の場合は必須扱いにしないこと。これらの場合、持家ヒアリング結果フィールドは非表示になります。

---

## 完了条件

- [ ] 受付日が2026-03-30以降、かつ問合時持家ヒアリングが空白でない場合に、持家ヒアリング結果が必須になる
- [ ] 問合時持家ヒアリング変更時に、持家ヒアリング結果の必須状態がリアルタイムで更新される
- [ ] 持家ヒアリング結果変更時に、必須状態がリアルタイムで更新される
- [ ] 初期表示時に、既に必須条件を満たしているが未入力のフィールドがハイライト表示される
- [ ] 問合時持家ヒアリングが「不要」または「未」の場合、持家ヒアリング結果フィールドが非表示になる
- [ ] 保存時に未入力必須フィールドがある場合、ValidationWarningDialog が表示される
- [ ] 既存の必須バリデーションが正常に動作する
- [ ] ビルドエラーが発生しない（UTF-8エンコーディングが保持されている）
