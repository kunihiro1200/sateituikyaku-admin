# Implementation Plan

- [x] 1. バグ条件探索テスト（修正前に実行）
  - **Property 1: Bug Condition** - 保存ボタンクリック時の白フラッシュ
  - **重要**: このテストは修正前のコードで実行し、白フラッシュが発生することを確認する
  - **目標**: 白フラッシュが発生することを確認し、バグの存在を証明する
  - **テスト内容**: 
    - 「不通・１番電話を保存」ボタンをクリック → 画面が一瞬白くなる（修正前のコードで失敗）
    - 「ステータスを更新」ボタンをクリック → 画面が一瞬白くなる（修正前のコードで失敗）
  - **期待される結果**: テストが失敗し、白フラッシュが発生することを確認
  - **確認方法**: ブラウザで通話モードページを開き、目視で白フラッシュを確認
  - _Requirements: 1.1, 1.2, 1.3_

- [x] 2. 保存テスト（修正前に実行）
  - **Property 2: Preservation** - コメント保存ボタンの動作維持
  - **重要**: 修正前のコードでコメント保存ボタンの動作を観察する
  - **テスト内容**:
    - コメント保存ボタンをクリック → 画面を維持したまま保存処理が行われる（修正前のコードで成功）
    - 保存処理が成功した時 → データベースに正しく保存され、スプレッドシートに同期される（修正前のコードで成功）
    - 保存処理が失敗した時 → エラーメッセージが表示される（修正前のコードで成功）
  - **期待される結果**: テストが成功し、現在の正常な動作を確認
  - **確認方法**: ブラウザで通話モードページを開き、コメント保存ボタンをテスト
  - _Requirements: 3.1, 3.2, 3.3_

- [-] 3. 売主リスト保存ボタン白フラッシュ修正

  - [x] 3.1 `handleSaveAndExit()`の修正
    - `loadAllData()`呼び出しを削除（行1666）
    - 保存済み値の更新のみを実行（`setSavedUnreachableStatus()`, `setSavedFirstCallPerson()`）
    - 成功メッセージの表示（`setSuccessMessage('保存しました')`）
    - ローディング表示を削除（`loadAllData()`削除により自動的に削除される）
    - コメント保存ボタンのパターンを適用（必要な状態のみを更新）
    - _Bug_Condition: isBugCondition(input) where input.buttonType == '不通・１番電話を保存' AND saveSuccessful(input) AND loadAllDataCalled(input)_
    - _Expected_Behavior: 保存処理後に画面を維持したまま、必要な状態のみを更新し、`loadAllData()`を呼び出さず、画面が白くならないこと_
    - _Preservation: コメント保存ボタンの既存動作（画面を維持したまま保存）を維持する_
    - _Requirements: 2.1, 2.2, 2.3, 3.1, 3.2, 3.3_

  - [x] 3.2 `handleSaveStatus()`の修正
    - `loadAllData()`呼び出しを削除（行1838）
    - 保存済み値の更新（`setStatusChanged(false)`、追加で保存したフィールドの値をローカルステートに反映）
    - 成功メッセージの表示（`setSuccessMessage('ステータスを更新しました')`）
    - ローディング表示を削除（`loadAllData()`削除により自動的に削除される）
    - コメント保存ボタンのパターンを適用（必要な状態のみを更新）
    - _Bug_Condition: isBugCondition(input) where input.buttonType == 'ステータスを更新' AND saveSuccessful(input) AND loadAllDataCalled(input)_
    - _Expected_Behavior: 保存処理後に画面を維持したまま、必要な状態のみを更新し、`loadAllData()`を呼び出さず、画面が白くならないこと_
    - _Preservation: コメント保存ボタンの既存動作（画面を維持したまま保存）を維持する_
    - _Requirements: 2.1, 2.2, 2.3, 3.1, 3.2, 3.3_

  - [x] 3.3 バグ条件探索テストの再実行（修正後）
    - **Property 1: Expected Behavior** - 保存ボタンクリック時の画面維持
    - **重要**: タスク1と同じテストを再実行する（新しいテストを書かない）
    - **テスト内容**:
      - 「不通・１番電話を保存」ボタンをクリック → 画面が白くならない（修正後のコードで成功）
      - 「ステータスを更新」ボタンをクリック → 画面が白くならない（修正後のコードで成功）
    - **期待される結果**: テストが成功し、白フラッシュが発生しないことを確認
    - **確認方法**: ブラウザで通話モードページを開き、目視で白フラッシュが発生しないことを確認
    - _Requirements: 2.1, 2.2, 2.3_

  - [x] 3.4 保存テストの再実行（修正後）
    - **Property 2: Preservation** - コメント保存ボタンの動作維持
    - **重要**: タスク2と同じテストを再実行する（新しいテストを書かない）
    - **テスト内容**:
      - コメント保存ボタンをクリック → 画面を維持したまま保存処理が行われる（修正後のコードで成功）
      - 保存処理が成功した時 → データベースに正しく保存され、スプレッドシートに同期される（修正後のコードで成功）
      - 保存処理が失敗した時 → エラーメッセージが表示される（修正後のコードで成功）
    - **期待される結果**: テストが成功し、現在の動作が維持されていることを確認
    - **確認方法**: ブラウザで通話モードページを開き、コメント保存ボタンをテスト
    - _Requirements: 3.1, 3.2, 3.3_

- [x] 4. Checkpoint - すべてのテストが成功することを確認
  - すべてのテストが成功したことを確認
  - 質問があればユーザーに確認
