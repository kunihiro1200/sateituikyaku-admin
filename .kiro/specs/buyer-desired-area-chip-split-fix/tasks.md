# Tasks: buyer-desired-area-chip-split-fix

## Task List

- [x] 1. バグ再現テストの作成（Exploratory）
  - [x] 1.1 `parseDesiredArea` ロジックを抽出したユーティリティ関数のテストファイルを作成する
  - [x] 1.2 読点を含むエリア名（例：「②中学校（滝尾、城東、原川）」）を入力として、未修正ロジックが3要素を返すことを確認するテストを書く（バグ再現）
  - [x] 1.3 テストを実行し、バグが再現されることを確認する

- [x] 2. バグ修正の実装
  - [x] 2.1 `frontend/frontend/src/pages/BuyerDesiredConditionsPage.tsx` の `fetchBuyer` 内（108行目付近）の `split('、')` フォールバックを削除し、`split('|')` のみに変更する
  - [x] 2.2 同ファイルの `handleInlineFieldSave` 後の `desired_area` 同期ロジック（196行目付近）も同様に修正する

- [x] 3. Fix Checking テストの作成・実行
  - [x] 3.1 修正後の `parseDesiredArea` ロジックに対して、読点含むエリア名が1要素になることを検証するテストを書く（Property 1）
  - [x] 3.2 `AREA_OPTIONS` の全エリア名を単独入力として渡し、常に1要素を返すことを検証するプロパティベーステストを書く
  - [x] 3.3 テストを実行し、全て通過することを確認する

- [x] 4. Preservation Checking テストの作成・実行
  - [x] 4.1 `|` 区切りの複数エリア値を入力として、修正後も正しい要素数を返すことを検証するテストを書く（Property 2）
  - [x] 4.2 空文字・null入力のエッジケーステストを書く
  - [x] 4.3 テストを実行し、全て通過することを確認する

- [x] 5. 動作確認
  - [x] 5.1 `getDiagnostics` で修正ファイルに型エラー・構文エラーがないことを確認する
  - [x] 5.2 ローカル環境で「②中学校（滝尾、城東、原川）」が1チップとして表示されることを手動確認する
  - [x] 5.3 複数エリアを選択した場合に各エリアが個別チップとして表示されることを手動確認する
