# 実装計画

- [x] 1. バグ条件探索テストを作成
  - **Property 1: Bug Condition** - 買主候補ボタンのパフォーマンス問題
  - **重要**: このテストは修正前のコードで失敗する必要があります - 失敗はバグの存在を確認します
  - **修正しないでください**: テストが失敗してもテストやコードを修正しないでください
  - **注意**: このテストは期待される動作をエンコードしています - 実装後にパスすることで修正を検証します
  - **目標**: バグの存在を示す反例を表面化させる
  - **スコープ付きPBTアプローチ**: 決定論的バグの場合、具体的な失敗ケースにプロパティをスコープして再現性を確保
  - 物件番号が存在する場合に買主候補ボタンをクリックした時、レスポンスタイムが3秒を超える（約50秒）ことをテスト
  - 期待される動作プロパティ（design.mdより）: レスポンスタイムが3秒以内であること
  - 修正前のコードでテストを実行
  - **期待される結果**: テストが失敗する（これは正しい - バグの存在を証明）
  - 根本原因を理解するために見つかった反例を文書化
  - テストが作成され、実行され、失敗が文書化されたらタスクを完了としてマーク
  - _Requirements: 1.1, 1.2_

- [x] 2. 保存プロパティテストを作成（修正実装前）
  - **Property 2: Preservation** - 既存機能の保持
  - **重要**: 観察優先の方法論に従う
  - 修正前のコードで非バグ入力（買主候補ボタン以外の操作）の動作を観察
  - 保存要件から観察された動作パターンをキャプチャするプロパティベーステストを作成
  - プロパティベーステストは多くのテストケースを生成し、より強力な保証を提供
  - 修正前のコードでテストを実行
  - **期待される結果**: テストがパスする（これはベースライン動作を保持することを確認）
  - テストが作成され、実行され、修正前のコードでパスしたらタスクを完了としてマーク
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_

- [x] 3. 買主候補パフォーマンス問題の修正

  - [x] 3.1 修正を実装
    - `backend/src/services/BuyerCandidateService.ts`の`getCandidatesForProperty()`メソッドを最適化
    - DBレベルでのフィルタリング強化（既に実装済みの場合は確認）
    - フィルタリング処理の最適化（JavaScriptでの処理を最小限に）
    - 距離マッチング処理の最適化（必要性を確認し、不要であれば削除）
    - 問い合わせ物件住所の取得最適化（`getPropertyAddressesInBatch()`メソッドのクエリ最適化）
    - `frontend/frontend/src/pages/PropertyListingDetailPage.tsx`の`handleOpenBuyerCandidates()`メソッドを修正
    - ウィンドウサイズの指定: `window.open()`の第3引数に`'width=1400,height=900,noopener,noreferrer'`を指定
    - _Bug_Condition: isBugCondition_Performance(input) where input.buttonId == 'buyer-candidates-button' AND responseTime > 3000ms_
    - _Bug_Condition: isBugCondition_WindowSize(input) where input.buttonId == 'buyer-candidates-button' AND windowSize < EXPECTED_SIZE_
    - _Expected_Behavior: responseTime <= 3000ms AND windowSize >= EXPECTED_SIZE from design_
    - _Preservation: 買主候補の抽出条件、テーブル表示形式、新しいタブで開く動作、他のボタンの動作が変わらないこと from design_
    - _Requirements: 1.1, 1.2, 1.3, 2.1, 2.2, 2.3, 3.1, 3.2, 3.3, 3.4, 3.5_

  - [x] 3.2 バグ条件探索テストが今度はパスすることを確認
    - **Property 1: Expected Behavior** - 買主候補ボタンのパフォーマンス改善
    - **重要**: タスク1と同じテストを再実行 - 新しいテストを作成しないでください
    - タスク1のテストは期待される動作をエンコードしています
    - このテストがパスすると、期待される動作が満たされていることを確認します
    - ステップ1からバグ条件探索テストを実行
    - **期待される結果**: テストがパスする（バグが修正されたことを確認）
    - _Requirements: design.mdの期待される動作プロパティ_

  - [x] 3.3 保存テストがまだパスすることを確認
    - **Property 2: Preservation** - 既存機能の保持
    - **重要**: タスク2と同じテストを再実行 - 新しいテストを作成しないでください
    - ステップ2から保存プロパティテストを実行
    - **期待される結果**: テストがパスする（リグレッションがないことを確認）
    - 修正後も全てのテストがパスすることを確認（リグレッションなし）

- [-] 4. チェックポイント - 全てのテストがパスすることを確認
  - 全てのテストがパスすることを確認し、質問があればユーザーに尋ねる
