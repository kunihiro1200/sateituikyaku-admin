# 実装計画

- [x] 1. バグ条件の探索的テストを作成する
  - **Property 1: Bug Condition** - 万円単位価格の誤った`/ 10000`変換
  - **重要**: このテストは未修正コードで**FAIL**することが期待される — FAILはバグの存在を証明する
  - **修正やコードを変更しないこと（FAILしても）**
  - **目的**: バグが存在することを示すカウンターサンプルを発見する
  - **スコープ**: 具体的な失敗ケースにスコープを絞る（`inquiry_price`が正の整数の場合）
  - `NearbyBuyersList`コンポーネントに`inquiry_price = 2380`を渡し、表示が`2,380万円`になることをアサートする（design.mdのBug Conditionより）
  - `inquiry_price = 500`を渡し、表示が`500万円`になることをアサートする
  - `inquiry_price = 10000`を渡し、表示が`10,000万円`になることをアサートする
  - 未修正コードで実行 → **期待される結果: FAIL**（`0.238万円`、`0.05万円`、`1万円`が表示される）
  - カウンターサンプルを記録する（例: `inquiry_price = 2380`のとき`2,380万円`ではなく`0.238万円`が表示される）
  - テストを作成・実行し、FAILを記録したらタスク完了とする
  - _Requirements: 1.1, 1.2, 2.1, 2.2_

- [x] 2. 保全プロパティテストを作成する（修正前に実施）
  - **Property 2: Preservation** - null/0価格の表示維持
  - **重要**: 観察優先メソドロジーに従う
  - 未修正コードで非バグ条件の入力（`inquiry_price`が`null`または`0`）の動作を観察する
  - 観察: `inquiry_price = null`のとき未修正コードで`-`が表示される
  - 観察: `inquiry_price = 0`のとき未修正コードで`-`が表示される（falsyとして扱われる）
  - プロパティベーステスト: `inquiry_price`が`null`または`0`の任意の入力に対して、表示が`-`になることを確認（design.mdのPreservation Requirementsより）
  - 未修正コードでテストを実行 → **期待される結果: PASS**（ベースライン動作を確認）
  - テストを作成・実行し、PASSを確認したらタスク完了とする
  - _Requirements: 3.1, 3.2, 3.3_

- [x] 3. 価格表示の`/ 10000`誤変換を修正する

  - [x] 3.1 修正を実装する
    - `frontend/frontend/src/components/NearbyBuyersList.tsx`の価格表示セルを修正する
    - `(buyer.inquiry_price / 10000).toLocaleString()`を`buyer.inquiry_price.toLocaleString()`に変更する
    - `/ 10000`の除去のみ — null/0チェックのロジックは変更しない
    - _Bug_Condition: `inquiry_price`が正の数値（万円単位）であるにもかかわらず`/ 10000`が適用されている状態（design.mdより）_
    - _Expected_Behavior: `inquiry_price`の値をそのまま万円として表示する（例: 2380 → `2,380万円`）_
    - _Preservation: `inquiry_price`が`null`または`0`の場合は引き続き`-`を表示する_
    - _Requirements: 2.1, 2.2, 3.1, 3.3_

  - [x] 3.2 バグ条件の探索的テストがPASSすることを確認する
    - **Property 1: Expected Behavior** - 万円単位価格の正しい表示
    - **重要**: タスク1で作成した**同じテスト**を再実行する — 新しいテストを書かないこと
    - タスク1のテストは期待される動作をエンコードしている
    - このテストがPASSすれば、期待される動作が満たされたことを確認できる
    - タスク1のバグ条件探索的テストを実行する
    - **期待される結果: PASS**（バグが修正されたことを確認）
    - _Requirements: 2.1, 2.2_

  - [x] 3.3 保全テストが引き続きPASSすることを確認する
    - **Property 2: Preservation** - null/0価格の表示維持
    - **重要**: タスク2で作成した**同じテスト**を再実行する — 新しいテストを書かないこと
    - タスク2の保全プロパティテストを実行する
    - **期待される結果: PASS**（リグレッションがないことを確認）
    - 修正後もすべてのテストがPASSすることを確認する

- [x] 4. チェックポイント — すべてのテストがPASSすることを確認する
  - すべてのテストがPASSすることを確認する。疑問点があればユーザーに確認する。
