# 実装計画

- [x] 1. バグ条件探索テストを作成
  - **Property 1: Bug Condition** - atbb_statusフィルタ欠如による誤表示
  - **重要**: このテストは修正前のコードで**必ず失敗する必要があります** - 失敗によりバグの存在が確認されます
  - **修正を試みないでください**: テストが失敗してもコードを修正しないでください
  - **注意**: このテストは期待される動作をエンコードしています - 実装後にパスすることで修正を検証します
  - **目標**: バグを実証する反例を表面化させる
  - **スコープ付きPBTアプローチ**: 決定論的バグのため、プロパティを具体的な失敗ケースにスコープして再現性を確保
  - 条件A OR 条件Bを満たすが、atbb_statusに"公開中"が含まれない買主が「一般媒介_内覧後売主連絡未」に分類されることをテスト（Bug Conditionから）
  - テストケース例：
    - 買主7145: atbb_status="非公開"、viewing_type_general="一般"、latest_viewing_date="2025-09-15"（過去）、post_viewing_seller_contact=空欄
    - 買主7150: atbb_status="専任・公開中"、post_viewing_seller_contact="未"
  - テストのアサーションは設計書のExpected Behavior Propertiesと一致させる（atbb_statusに"公開中"が含まれない買主は除外されるべき）
  - 修正前のコードでテストを実行
  - **期待される結果**: テスト失敗（これは正しい - バグの存在を証明）
  - 反例を文書化して根本原因を理解する
  - テストが作成され、実行され、失敗が文書化されたらタスク完了とする
  - _Requirements: 1.1, 1.2_

- [x] 2. 保存プロパティテストを作成（修正実装前）
  - **Property 2: Preservation** - 他のカテゴリーのフィルタリング維持
  - **重要**: 観察優先の方法論に従う
  - 観察: 修正前のコードで非バグ入力（他のサイドバーカテゴリー）の動作を観察
  - 観察: Priority 1-7, 9-35のカテゴリーは正常に動作している
  - 観察: atbb_statusに"公開中"が含まれ、条件A OR 条件Bを満たす買主は正しく「一般媒介_内覧後売主連絡未」に分類される
  - Preservation Requirementsから、全ての非バグ条件入力で動作が保持されることをproperty-based testで記述
  - プロパティベーステストは多くのテストケースを自動生成し、より強力な保証を提供
  - 修正前のコードでテストを実行
  - **期待される結果**: テスト成功（ベースライン動作を確認）
  - 修正前のコードでテストがパスしたらタスク完了とする
  - _Requirements: 3.1, 3.2_

- [x] 3. 「一般媒介_内覧後売主連絡未」カテゴリーのatbb_statusフィルタバグの修正

  - [x] 3.1 BuyerStatusCalculatorのPriority 8ロジックを修正
    - 条件Aに`contains(buyer.atbb_status, '公開中')`を追加
    - 条件Bに`contains(buyer.atbb_status, '公開中')`を追加
    - コメントを更新して新しいロジックを説明
    - matchedConditionに「（公開中のみ）」を追加
    - getDiagnosticsでTypeScriptエラーがないことを確認
    - _Bug_Condition: (conditionA OR conditionB) AND NOT contains(buyer.atbb_status, '公開中')_
    - _Expected_Behavior: atbb_statusに"公開中"が含まれる買主のみが「一般媒介_内覧後売主連絡未」に分類される_
    - _Preservation: 他のサイドバーカテゴリー（Priority 1-7, 9-35）のフィルタリングロジックは変更されない_
    - _Requirements: 1.1, 1.2, 2.1, 2.2, 3.1, 3.2_

  - [x] 3.2 バグ条件探索テストが成功することを確認
    - **Property 1: Expected Behavior** - atbb_statusフィルタの適用
    - **重要**: タスク1と同じテストを再実行 - 新しいテストを書かない
    - タスク1のテストは期待される動作をエンコードしている
    - このテストがパスすれば、期待される動作が満たされていることを確認
    - タスク1のバグ条件探索テストを再実行
    - **期待される結果**: テスト成功（バグが修正されたことを確認）
    - _Requirements: 2.1, 2.2_

  - [x] 3.3 保存テストが引き続き成功することを確認
    - **Property 2: Preservation** - 他のカテゴリーのフィルタリング維持
    - **重要**: タスク2と同じテストを再実行 - 新しいテストを書かない
    - タスク2の保存プロパティテストを再実行
    - **期待される結果**: テスト成功（リグレッションがないことを確認）
    - 修正後も全てのテストがパスすることを確認（リグレッションなし）

- [x] 4. チェックポイント - 全てのテストがパスすることを確認
  - 全てのテストがパスすることを確認し、疑問があればユーザーに質問する

